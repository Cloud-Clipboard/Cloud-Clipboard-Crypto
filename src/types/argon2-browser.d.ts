/**
 * Ensures that the argon2-browser module is correctly typed when imported
 * as minified version.
 */
declare module "argon2-browser/dist/argon2-bundled.min.js" {
  export * from "argon2-browser";
}
